services:
  lunch:
    container_name: lunch
    image: localhost/jdobes/lunch:latest
    build:
      context: .
      dockerfile: Containerfile
    ports:
      - "8000:8000"
    environment:
      - FACEBOOK_COOKIE=
      - GEMINI_API_KEY=
